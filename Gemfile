source "https://rubygems.org"

git_source(:github) {|repo_name| "https://github.com/#{repo_name}" }

# Specify your gem's dependencies in fraudDetector.gemspec
gemspec

group :development, :test do
  gem 'rspec-rails', '~> 3.7'
end